{
  "name": "restaurant-management-system",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend_server/app.js",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "heroku-postbuild": "npm run build && cd backend_server && npm install",
    "postinstall": "cd backend_server && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": "18.x", 
    "npm": "8.x"   
  }
}